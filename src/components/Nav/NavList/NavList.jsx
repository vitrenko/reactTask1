import style from './NavList.module.scss';

const NavList = () => {
    return (
        <nav>
            <ul className={style.navList}>
            </ul>
        </nav>
    )
};

export default NavList;